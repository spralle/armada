---
"@ghost-shell/shell-react": minor
"@ghost-shell/shell-dom": minor
---

Extract React shell host bindings into a new publishable `@ghost-shell/shell-react` package and rewire the shell app host to consume it, while keeping compatibility shims in `apps/shell` during migration.
