---
"@ghost-shell/shell-dom": minor
---

Add a new publishable `@ghost-shell/shell-dom` package that exposes the DOM runtime bootstrap entrypoint (`startShell`) while consuming `@ghost-shell/shell-core`.
