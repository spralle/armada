---
"@ghost-shell/shell": minor
---

Extract `@ghost-shell/shell` package with batteries-included `createGhostShell()` API for embedding the ghost shell. External apps can now mount a complete shell with one call including plugin hydration, theming, and HMR support.
