import { definePlugin } from "@ghost-shell/contracts";

export const pluginManifest = definePlugin({
  id: "ghost.__PLUGIN_NAME__",
  name: "__PLUGIN_TITLE__",
  version: "0.1.0",
  contributes: {
    parts: [
      {
        id: "__PART_ID__",
        label: "__PLUGIN_TITLE__",
      },
    ],
  },
});
