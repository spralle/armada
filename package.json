{
  "name": "armada",
  "private": true,
  "packageManager": "bun@1.2.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run check:architecture-guardrails --silent && tsc --pretty false -p tsconfig.json --noEmit",
    "typecheck": "tsc --pretty false -p tsconfig.json --noEmit",
    "test": "npm run build --silent && tsc --pretty false -p apps/shell/tsconfig.test.json && node --test packages/plugin-contracts/test/parse-plugin-contract.test.mjs apps/shell/test/plugin-loader-parity.test.mjs apps/shell/test/plugin-registry-parity.test.mjs",
    "build": "tsc -b --pretty false",
    "check:architecture-guardrails": "node ./scripts/check-architecture-guardrails.mjs",
    "dev:shell": "npm run dev --workspace @armada/shell-app",
    "dev:backend": "node --watch --experimental-strip-types apps/backend/src/index.ts",
    "dev:plugin": "npm run dev --workspace @armada/plugin-starter",
    "dev:plugin-sample": "npm run dev --workspace @armada/sample-contract-consumer-plugin",
    "dev:plugin:all": "npx concurrently -n starter,sample -c magenta,cyan \"npm run dev:plugin\" \"npm run dev:plugin-sample\"",
    "dev:integration": "bun --watch apps/shell/src/index.ts",
    "scaffold:plugin": "node ./scripts/scaffold-plugin.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.8.3"
  }
}
